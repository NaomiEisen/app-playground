name: Test Arguments & Failure

on: [workflow_dispatch]

jobs:
  # Job 1: Should PASS and print your message
  test-passing-arguments:
    uses: NaomiEisen/workloads/.github/workflows/reusable-ci.yml@main
    with:
      message_to_print: "Hello from the success path! 🚀"
      simulate_error: false
    secrets: inherit

  # Job 2: Should FAIL and block the PR
  test-failure-detection:
    uses: NaomiEisen/workloads/.github/workflows/reusable-ci.yml@main
    with:
      message_to_print: "I am about to crash..."
      simulate_error: true
    secrets: inherit